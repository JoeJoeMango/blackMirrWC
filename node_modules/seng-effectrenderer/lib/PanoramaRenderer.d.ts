import sengDisposable from 'seng-disposable';
export default class PanoramaRenderer extends sengDisposable {
    private imageEffectRender;
    private mouseListener;
    private rotateX;
    private rotateY;
    private rotateSpeedX;
    private rotateSpeedY;
    private inertia;
    private smoothness;
    private fovV;
    private isReady;
    private animationLoop;
    constructor(canvasParent: HTMLElement, image: HTMLImageElement | HTMLVideoElement | HTMLCanvasElement, fovDegrees?: number, rotateInertia?: number, smoothness?: number);
    init(): void;
    play(): void;
    pause(): void;
    updateSize(): void;
    updateImage(image: HTMLImageElement | HTMLVideoElement | HTMLCanvasElement, clampHorizontal?: boolean, clampVertical?: boolean): void;
    private update();
    private getShader();
    private lerp(a, b, i);
    dispose(): void;
}
